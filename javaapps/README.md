[CONSOLE APPLICATIONS]

ASCII_Rectangle.java - A utility that generates a visual ASCII art rectangle based on user-specified height and width dimensions, rendering the shape using asterisk characters to the console.

AreaPerimeter.java - A geometric calculator that computes and displays both the area and perimeter of a rectangle derived from user-provided length and width input values.

AudioPlayerExample.java - An audio demonstration application that loads and plays a specific waveform audio file ("Blip.wav") using the Java Sound API's Clip interface.

BabySitterPayCalc.java - A payroll calculator for babysitting services that computes total earnings for multiple employees based on hours worked and number of children managed, generating a formatted compensation report.

BankMainApp.java - A banking console simulation that manages a user's account balance, supporting deposit and withdrawal transactions through an interactive command-line interface.

BasicNumberSorter.java - A data organization tool that accepts a sequence of five integer inputs and utilizes standard sorting algorithms to output the numbers in ascending numerical order.

CarpetPriceEstimator.java - A construction cost estimation tool that calculates the total material cost for carpeting a room, converting input dimensions from feet to square yards and applying a fixed unit price.

CelebNameSlicer.java - A string manipulation demonstration that processes a list of names by programmatically trimming specific leading and trailing characters to exhibit substring functionality.

CharacterCounter.java - A text analysis utility that scans a user-provided phrase to calculate and report the exact frequency of a specific target character within the text.

CircleArea.java - A mathematical tool that computes the precise area of a circle based on a user-submitted radius value, formatting the result to two decimal places.

ConsoleCalc.java - An arithmetic processor acting as a basic calculator, enabling users to perform addition, subtraction, multiplication, or division on two numerical operands via a menu-driven interface.

DOSNotepad2.java - A comprehensive command-line text editor modeled after VIM, featuring modal operation for text insertion, buffer viewing, file saving, and content clearing.

DateFormatter.java - A date parsing utility that converts raw numerical date inputs (month, day, year) into a formal localized string format (e.g., "January 25, 2010").

DigitFrequencyCounter.java - A statistical analysis tool that parses a string of numeric input to determine the distribution frequency of each digit (0-9) present in the sequence.

EvenOddChecker.java - A parity analysis tool that evaluates an integer input to determine its classification as either an even or odd number using modular arithmetic.

FibonacciRecursive.java - A mathematical demonstration that generates and displays the first N terms of the Fibonacci sequence using a recursive algorithmic approach based on user input.

FileIODemo.java - A file management utility that demonstrates reading from and writing to text files, allowing usage as a simple contact storage system (Name, DOB) with persistence checks.

FileTokenizerDemo.java - A parsing utility that opens a user-specified text file and utilizes the `StringTokenizer` class to break down and display the content as individual tokenized words.

GPACalculator.java - An academic grading tool that computes a precise Grade Point Average (GPA) by converting a series of seven input letter grades into their numerical equivalents.

GuessTheNumber.java - An interactive command-line game where the user attempts to guess a randomly generated number between 1 and 10, featuring high/low hints and a replay mechanism.

JavaNano.java - A persistent console-based text editor simulating the Nano interface, supporting file loading, editing, saving, and command-driven navigation (search, print, exit).

LetterSpan.java - A linguistic calculation tool that determines the alphabetical distance (or absolute span) between two user-provided characters and lists all letters falling within that range.

ListDemo.java - A data structure demonstration that allows users to populate a dynamic list with string entries via the console until a termination command is issued, subsequently displaying all collected items.

LittleMathMethods.java - A modular arithmetic calculator that accepts two numerical inputs and processes them through distinct methods to perform and display addition, subtraction, multiplication, and division results.

MathOperations.java - A comprehensive demonstration of the Java `Math` library, performing various operations such as square root, exponentiation, absolute value, and random number generation on user inputs.

MatrixMultiplier.java - A linear algebra tool that enables users to input two separate dimensions and data sets for matrices, validating their compatibility before computing their product.

MatrixOperations.java - A matrix arithmetic calculator offering a menu to perform matrix addition, scalar addition, or scalar multiplication on user-defined integer matrices.

MethodSquare.java - A simple functional demonstration that accepts an integer input and passes it to a dedicated method to calculate and return its square value.

MorseCodeTranslator.java - A translation utility that converts alphanumeric text input into Morse code (dots and dashes) using parallel lookup arrays for character mapping.

MovingCostCalculator.java - A logistics calculator that estimates moving service costs based on distance traveled, applying different rate structures for local, standard, and long-distance tiers.

MultiplesOf3And5.java - A numerical analysis solution (solving Project Euler Problem 1) that sums all natural numbers below a user-defined limit that are multiples of 3 or 5.

NamePrefixer.java - A string processing utility that iterates through a list of names and intelligently appends gender-specific titles (Mr./Ms.) based on a predefined set of known names.

NumberFileSorter.java - A robust data processing tool that attempts to read integers from a file ("numbers.txt") or accepts manual fallback input, then sorts and displays the collection.

NumberFormatter.java - A formatting demonstration that illustrates the use of the `DecimalFormat` class to render large floating-point numbers with custom separators and precision.

NumberRangePrinter.java - A sequence generator that accepts two integer bounds from the user and prints all integers strictly greater than the start value up to and including the end value.

NumericalSorter.java - A sorting utility that prompts the user for a count of numbers, accepts that many integer inputs, sorts them in ascending order using `Arrays.sort`, and displays the result.

PalindromeChecker.java - A text verification tool that recursively checks if a given string reads the same forwards and backwards, automatically filtering punctuation and ignoring case.

PasswordValidator.java - A security simulation that tests user-submitted passwords against three criteria: presence of digits, absence of repeated characters squences, and inclusion of special characters.

PatternGenerator.java - A nested-loop demonstration that generates a block of text patterns by iterating through vowel and consonant strings to produce combinatorial output.

PhoneNumberConverter.java - A translation utility that converts alphanumeric phone numbers (e.g., "1-800-FLOWERS") into their standard numeric equivalent based on the telephone keypad mapping.

PlanetaryWeightConverter.java - An astronomical calculator that computes a user's relative weight on different fictional planets (Voltar, Krypton, etc.) based on predefined gravitational factors.

PlayerNameSubstrings.java - A string manipulation demo that parses full names (e.g., "First Last") to extract and display the first and last names separately using index-based substring slicing.

ProductTotalCalculator.java - A retail calculator that sums the prices of a user-specified number of products and calculates the final total including a 6% sales tax.

RandomAlphabet.java - A randomization tool that generates and prints a unique permutation of the 26-letter English alphabet, ensuring no characters are repeated in the output.

RangePrinter.java - A loop demonstration that accepts start and end integers from the user and sequentially prints every integer within that inclusive range.
 
ReadStdinInt.java - A simple demonstration of reading integer input from the standard input stream using BufferedReader, including error handling for non-integer inputs.
 
RectangleDrawer.java - Draws a solid rectangle of asterisks (*) to the console based on user-supplied height and width dimensions.
 
SalesTaxCalculator.java - interactive program that calculates the total cost of a shopping list. It asks for the number of items and the price of each, then computes the subtotal, a 6% sales tax, and the final total price.

SecurityTextScanner.java - Scans a named text file ("cbp1data.txt") for specific security-related keywords (e.g., "bomb", "gun") and flags any lines containing them for review.

SimpleIO.java - A helper class demonstrating cleaner input/output methods, wrapping the Scanner class to provide one-line prompt-and-read functionality.

SimpleLoop.java - A basic loop demonstration that asks the user for a repetition count and then prints a specific message that many times.

SimpleLottery.java - A simple luck-based game where the user tries to match two randomly generated numbers (0-4). Includes a replay loop.

SimplePalindrome.java - Checks if a user-provided string is a palindrome (reads the same forwards and backwards), ignoring spaces and case.

SmallestMultiple.java - A search algorithm that finds the smallest positive number that is evenly divisible by all integers from 1 to 20.

SortedNumberList.java - Accepts a user-defined quantity of numbers (entered as strings), sorts them numerically/alphabetically, and prints the sorted list.

StringArrayDemo.java - Demonstrates storing multiple user inputs in a String array and then iterating through the array to display them.

StringDemo.java - A demonstration of manual string reversal using character arrays, operating on a hardcoded palindrome string.

StringReverser.java - Takes a user input string and prints it in reverse order using the StringBuilder class for efficiency.

StringRotator.java - performing a "right rotation" on a string based on a numeric suffix (e.g., "Hello2" becomes "loHel").

StringedArraySorter.java - Collects a user-specified number of strings, sorts them alphabetically using standard library functions, and prints the result.

Strtok.java - Demonstrates the use of StringTokenizer to split a sentence or phrase into individual words (tokens).

SumOfSquares.java - multiple numbers, summing their squares. It specifically calculates the sum of squares for numbers 1 to 10 in the provided code.

TextAnalyzer.java - Analyzes a text string to report statistics including vowel counts, total word count, letter count, and punctuation usage.

TreePrinter.java - Generates a pyramid-shaped "tree" of asterisks based on a user-supplied odd integer height.

TriangleArea.java - Calculates the area of a triangle given the coordinates of its three vertices using the Shoelace Formula.

VowelCounter.java - Counts the occurrences of each vowel (A, E, I, O, U) in a user-provided phrase, case-insensitive.

WordSearch.java - A solver for word search puzzles. Reads a puzzle grid and a dictionary from files, then searches for words in all 8 directions.

WordSorter.java - Continually accepts words from the user until "done" is typed, then sorts and displays the list of entered words.

ascii.java - Prints a reference table of ASCII characters and their corresponding numeric values from 1 to 128.

nameLength.java - A string utility that takes a name input, calculates its length, and prints the name in reverse.

number10.java - Calculates the area of a circle from a given radius and demonstrates a manual method for rounding the result to two decimal places.

reversenumbers.java - A mathematical curiosity that reverses two integer inputs, adds them together, and then displays the reversed digits of that sum.

teacher.java - A simple gradebook application allowing a teacher to input student names and numerical grades, which are then converted to letter grades (A-F) and displayed.

[GUI APPLICATIONS]

AddThis.java - A basic Swing GUI application that takes two integer inputs in text fields and displays their sum upon clicking a button.
 
AnalogClock.java - Renders a functional 24-hour analog clock using Java 2D graphics, featuring second, minute, and hour hands that update in real-time.
 
BMICalculator.java - A health utility providing a GUI to calculate Body Mass Index (BMI) based on user-inputted height (inches) and weight (pounds).
 
BSOD.java - A "prank" application simulating the Windows Blue Screen of Death, which initiates a system shutdown after a 2-minute timer (cancelable via Escape key).
 
BallMover.java - Displays a smooth animation of a yellow ball bouncing horizontally back and forth within a blue window.
 
BankHelper.java - A helper class for banking applications, providing methods for account management and displaying balance alerts via JOptionPane dialogs.
 
Calculator.java - A fully functional AWT-based calculator supporting standard arithmetic operations (add, subtract, multiply, divide) and copy-paste functionality.
 
ColorArcs.java - A graphical demonstration that draws multiple concentric arcs using generated color gradients (grayscale and red-green ranges).
 
ColorCheckBoxWindow.java - An interactive window allowing the user to toggle the background color (Yellow/White) and text color (Red/Black) using checkboxes.
 
DecimalConverterGUI.java - A comprehensive number system converter allowing transformation between Decimal, Binary, Octal, and Hexadecimal formats via radio button selection.

GeometryCalc.java - Calculates and displays geometric properties (Circumference, Area, Surface Area, Volume) for circles and spheres based on a user-defined radius.
 
HackerTerminalSimulator.java - An immersive "movie-style" hacker terminal simulation featuring fake commands (trace, hack, nuke) and a scrolling command history.
 
JFrameExample.java - A basic introductory GUI example featuring a text input field, a "Click ME" button that greets the user, and non-functional usage of radio buttons.
 
JavaMathGUI.java - A math exploration tool that takes a number input and simultaneously displays the results of various Math class operations (Square Root, Power, Rounding, etc.).
 
JavaNotepad.java - A fully functional text editor supporting standard file operations (New, Open, Save, Print) and word wrapping, mimicking the classic Windows Notepad.
 
JumbledImagePuzzle.java - A visual puzzle game that loads an image (defaulting to a dog), splits it into tiles, and allows the user to click a "Jumble" button to scramble them.
 
KeyGenerator.java - Generates pseudo-random software license keys by calculating massive Fibonacci numbers and converting subsets of them into formatted Hexadecimal strings.
 
MidiMusic.java - A synthesized music player that programmatically generates and plays retro-style background music tracks using Java's MIDI system.
 
MorseCodeGUI.java - Provides a graphical interface for translating user-typed text into visual Morse Code sequences (dots and dashes).
 
NumberSystemConverter.java - A multi-base conversion tool where the user selects two checkboxes (Source and Target) to convert values between Decimal, Binary, Octal, and Hexadecimal systems.

Pong.java - An implementation of the classic arcade game Pong, featuring keyboard-controlled paddle movement (A/S keys) and wall-bouncing physics.
 
Problem1GUI.java - A specific logic checker that determines if an input number is considered "easy" based on whether it contains exactly one type of restricted digit (2, 3, 4, 5, 6, 8, 9).
 
PythagoreanTriples.java - searches for and lists sets of integers (a, b, c) that satisfy the Pythagorean theorem (a^2 + b^2 = c^2) within a specified range (1-100).
 
RandomColorSquares.java - Creates a visual "flashing" effect by rapidly drawing randomly sized and colored squares at random positions on the canvas.
 
RandomDots.java - An automated drawing program that periodically adds a new randomly colored circle to the window every 500 milliseconds.
 
RandomLineDrawer.java - Accumulates a drawing of random red lines on a white background, using a buffered image to preserve previously drawn lines.
 
RandomPixels.java - A high-speed graphics demo that fills the screen with thousands of randomly placed and colored small ovals ("pixels").
 
RandomWalkAnimation.java - Simulates a "Random Walk" algorithm on a grid, visualizing the path of an agent moving in random directions (Up, Down, Left, Right).
 
RedWhiteBlue.java - A simple interactive window with three buttons that change the background color of the application to Red, White, or Blue upon clearing.
 
SimpleNotepad.java - A basic text editor facilitating opening and saving text files via a graphical interface.
 
SimpleSpaceGame.java - A visual simulation featuring a bouncing text label and rectangle that continuously reflects off the window edges.
 
SmartNotepad.java - An enhanced text editor equipped with an autocomplete feature that suggests words from a predefined dictionary as you type.
 
StudentRecordApp.java - A data entry application for managing student records (Name, Grade, DOB) with capabilities to save to and load from a local file.
 
SystemBeep.java - A simple utility that triggers the system's auditory beep or bell signal.
 
TriangleClassifierApp.java - An AWT-based tool that accepts three side lengths to determine if they form a valid triangle and classifies it by type.
 
TriangleValidator.java - A Swing-based variation of a triangle classifier that validates side lengths and identifies Right Triangles.
 
Welcome.java - A digital welcome screen displaying the current date and time alongside a puppy image fetched dynamically from the internet.
 
inputGUIcalc.java - A simple calculator enabling basic arithmetic operations (Add, Subtract, Multiply, Divide) on two input numbers.
 
reverse.java - A utility that accepts a text string from the user and outputs the reversed version of that string.
 
SevenEleven/SevenEleven.java - A graphical dice game where the player attempts to roll a sum of 7 or 11 within three attempts to win.
 
dice/dice2.java - A dice simulation program that generates random dice rolls and visually displays the results using images.
 
dice/escape.java - A console demonstration of various Java string escape sequences such as newlines, tabs, and alerts.
 
jpong/jPong.java - A comprehensive Arkanoid-style arcade game featuring a complete menu system, multiple difficulty levels, sound effects, and brick-breaking gameplay mechanics.






